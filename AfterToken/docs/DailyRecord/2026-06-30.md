# 2026-06-30

## 工作内容

1. **项目进度全面盘点**
   - 通读项目根目录文档：`CONTEXT.md`、`TODO.md`、`AGENT.md`、`CLAUDE.md`。
   - 通读 `docs/` 下 `framework/`、`modules/`、`standards/`、`adr/`、`CoWork/`、`notice/` 等目录文档，以及 `俯视角2D射击游戏功能设计.md`。
   - 阅读 `.claude/skills/` 与 `.codex/skills/` 下全部 skill 文件，梳理项目定义的 skill 用途与工作流。
   - 探索代码结构：`Assets/GameScripts/`、`Assets/GameScripts/HotFix/GameLogic/`、`Assets/GameScripts/HotFix/GameProto/`、`Assets/TEngine/`、`Assets/Launcher/`、`Assets/Editor/`、`Assets/Scenes/`、`Configs/GameConfig/`、`Tools/Luban/`。

2. **当前进度总结**
   - 确认项目处于 **M1「战斗闭环」** 阶段。
   - 战斗核心循环（移动 → 瞄准 → 射击 → 命中 → 死亡）已实现，但整体仍为可验证原型。
   - 已完成：热更/资源管线、主包与热更域流程、输入/武器/弹道/飞行物/辅助瞄准/玩家 FSM/战斗/命中反馈/相机基础/UI Prefab 工作流/LoadingUI/Luban 工程搭建。
   - 进行中/待完善：Luban 表数据补充、玩家/武器/敌人/关卡系统配置接入、事件系统补齐、对象池拆分、相机抖动、敌人 AI/波次/胜负。
   - 未开始：音频/特效、掉落/拾取、奖励/共享/存档/设置、模拟经营系统。
   - 关键阻塞：Luban 配置表数据补充、事件系统完善、敌人 AI/波次。

3. **按原有格式同步模块进度文档**
   - 更新 `docs/modules/pipeline/luban-config-system/progress.md`：将 Luban 配置表系统状态从「待本地运行验证」调整为「生成逻辑已跑通，仅缺数据补充」。
   - 更新 `docs/modules/pipeline/config-system/progress.md`：同步为配置工程已搭建、JSON 输出已切换、生成逻辑已通，待补充表数据。
   - 更新各战斗模块 `progress.md`，细化进行中/待办项，统一状态标记。
   - 更新 `docs/TODO.md` 模块状态总览、阻塞链、本周聚焦与变更记录。

4. **敌人头顶血条实现**
   - 修改 `EnemyEntity.cs`：
     - 增加 `HealthBarRoot/Background/Fill` 血条结构。
     - `EnsureHealthBar()` 优先使用 Prefab 中已配置的血条节点，未配置则运行时动态创建占位血条。
     - `UpdateHealthBar()` 根据血量比例缩放填充条，并按阈值切换颜色（绿/黄/红）。
     - 血条默认位于敌人头顶上方 `0.6` 单位处。
   - 修改 `BattleSceneSetup.cs`：
     - `CreateEnemyPrefab()` 中新增 `CreateEnemyHealthBar()`，在 `Enemy.prefab` 中创建血条结构。
   - 同步更新 `docs/modules/combat/enemy-system/README.md`、`progress.md` 与 `docs/射击模块实现文档.md` 中 `EnemyEntity` 与编辑器工具章节。
   - 需在 Unity 中运行 `Battle/Setup Battle Scene & Resources` 重新生成 `Enemy.prefab` 与模拟构建清单。

5. **自动换弹与换弹准星**
   - 修改 `WeaponInstance.cs`：
     - `Fire()` 在弹匣打空后自动调用 `Reload(ownerId)`。
     - `Reload(ownerId)` 开始/完成时广播 `IWeaponEvent.OnReloadStateChanged(ownerId, isReloading)`。
   - 修改 `IWeaponEvent.cs`：新增 `OnReloadStateChanged` 事件。
   - 修改 `WeaponSystem.cs`：手动换弹时传递 `ownerId`。
   - 修改 `BattleMainUI.cs`：
     - 新增 `CrosshairStyle.Reloading` 与缺口圆环 Sprite 生成。
     - 监听 `OnReloadStateChanged`，换弹时切换为转圈准星，完成后恢复。
   - 修改 `CrosshairUpdater.cs`：换弹期间持续旋转准星。
   - 同步更新 `docs/modules/combat/weapon-system/README.md`、`progress.md` 与 `docs/射击模块实现文档.md`。

6. **修复镜头跟随卡顿**
   - 问题原因：`CameraSystem` 原先依赖 `PlayerEntity.FixedUpdate` 中广播的 `OnPlayerPositionChanged` 事件更新目标位置，而 `FixedUpdate` 频率低于 `LateUpdate`，导致相机目标位置更新不平滑。
   - 修改 `CameraSystem.cs`：
     - `LateUpdate` 中直接读取 `PlayerSystem.Instance.GetPlayerEntity().transform.position` 作为跟随目标。
     - 新增 `CameraFollowMode` 枚举：`Hard`（零延迟硬跟随，默认）、`Exponential`（指数平滑，无速度上限）、`SmoothDamp`（传统平滑）。
     - 新增 `_lookAheadFactor` 根据玩家速度加入提前量。
   - 修改 `PlayerEntity.cs`：
     - `ResetEntity()` 中设置 `_rb.interpolation = RigidbodyInterpolation2D.Interpolate`，让 `transform.position` 在渲染帧经过插值。
     - 将 `transform.up` 朝向更新从 `FixedUpdate` 移到 `Update`，保证视觉朝向流畅。
   - 修改 `BattleSceneSetup.cs`：`CreatePlayerPrefab()` 中设置 `rb.interpolation = RigidbodyInterpolation2D.Interpolate`。
   - 同步更新 `docs/modules/combat/camera-system/README.md`、`progress.md` 与 `docs/射击模块实现文档.md`。

7. **光标显示/隐藏管理**
   - 新增 `CursorManager.cs`：
     - 引用计数管理 `ShowCursor()` / `HideCursor()`。
     - 强制模式 `ForceShowCursor()` / `ForceHideCursor()`。
     - `Free` / `Locked` 两种光标锁定模式。
   - 修改 `MainMenuUI.cs` / `LobbyUI.cs` / `WeaponWheelUI.cs`：在 `OnCreate` 中显示光标，在 `OnDestroy` 中隐藏光标。
   - 修改 `ProcedureMainMenu.cs` / `ProcedureLobby.cs`：进入时 `ForceShowCursor()` + `SetLockMode(Free)`。
   - 修改 `ProcedureBattle.cs`：进入时 `ForceHideCursor()` + `SetLockMode(Locked)`。
   - 新增 `docs/modules/ui/cursor-system/README.md` 与 `progress.md`，并更新 `docs/modules/README.md` 索引。
   - 同步更新 `docs/射击模块实现文档.md`。

8. **光标资源可配置化**
   - 修改 `CursorManager.cs`：
     - 新增 `SetDefaultCursor(texture, hotSpot)` 与 `SetCursor(texture, hotSpot)`，支持自定义 `Texture2D` 光标纹理。
     - `ApplyCursorState()` 在显示光标时调用 `Cursor.SetCursor()`，隐藏时恢复系统默认。
   - 修改 `MainMenuUI.cs`：
     - 新增 `SetupDefaultCursor()`，运行时生成默认箭头纹理并设置为默认光标。
     - 文档说明后续可将纹理放到 `Assets/AssetRaw/Textures/Cursor_Default.png`，改为 YooAsset 加载。
   - 同步更新 `docs/modules/ui/cursor-system/README.md`、`progress.md` 与 `docs/射击模块实现文档.md`。

9. **Console 报错修复**
   - 问题：`CursorManager.cs` 中自定义枚举 `CursorLockMode` 与 `UnityEngine.CursorLockMode` 冲突，导致编译错误：
     - `'CursorLockMode' does not contain a definition for 'None'`
     - `ProcedureLobby.cs` / `ProcedureMainMenu.cs` 等引用处同样报错。
   - 修复：将自定义枚举重命名为 `GameCursorLockMode`，并同步更新所有引用。
   - 查看 `Editor.log` 确认编译已通过（`Tundra build success`），当前无新增代码报错。

10. **修复光标锁死在屏幕中心**
    - 问题：从战斗（Locked）返回菜单/大厅或打开武器轮盘时，光标会锁死在屏幕中心无法移动。
    - 根因：Windows 下 `Cursor.lockState` 从 `Locked` 切回 `None` 需要约一帧才能真正释放鼠标；原代码在不可见时提前解锁，导致显示光标时 Windows 尚未完成解锁。
    - 修复：
      - 修改 `CursorManager.ApplyCursorState`：
        - Free 模式下且光标不可见时，不再提前修改 `Cursor.lockState`，保持当前锁定状态。
        - 显示光标时，若当前仍处于 `Locked`，先设置 `Cursor.lockState = None`，再通过 `UniTask.Yield()` 等待一帧，最后设置 `Cursor.visible = true`。
        - 增加 `CancellationTokenSource` 取消上一次延迟应用，避免连续调用导致状态竞争。
      - 注释 `ProcedureMainMenu.cs` 中编辑器的自动跳转战斗调试代码，避免测试主菜单/大厅时光标被立即切回 Locked。

---

## 当前项目状态速览

| 领域 | 状态 |
|---|---|
| 热更/资源管线 | ✅ 基本完成 |
| 流程系统（主包 + 热更域） | ✅ 完成 |
| 输入/武器/弹道/飞行物/辅助瞄准 | ✅ 完成 |
| 玩家系统 | 🟡 基础版，待接入 TbPlayer |
| 敌人系统 | 🟡 占位，待 AI/FSM |
| 关卡系统 | 🟡 硬编码 + TbLevel，待波次/胜负 |
| 战斗/伤害系统 | 🟡 基础版，待暴击/Buff/结果事件 |
| 相机系统 | 🟡 基础版，待抖动细节 |
| UI 系统 | ✅ 基础完成 |
| Luban 配置表系统 | 🟡 生成逻辑已跑通，缺数据 |
| 音频/特效/掉落/奖励/共享/存档/设置 | ⏳ 未开始 |
| 模拟经营系统 | ⏳ 未开始 |

---

## 后续计划

- 用户后续补充 Luban 配置表数据（`item`、`player`、`enemy`、`wave`、`drop`、`buff` 等表）。
- 待数据补充后，推进：
  1. 替换硬编码 `WeaponConfigMgr` / `LevelConfigMgr` 为 Luban 表。
  2. 补齐 `ILevelEvent`、`IBattleResultEvent`、共享层事件接口。
  3. 实现敌人 FSM、波次生成、胜负判定。
  4. Play Mode 全流程验证 `MainMenu → Lobby → Battle`。
