{
    "endpoint": "https://api.deepseek.com/v1",
    "apiKey": "sk-your-key-here",
    "model": "deepseek-chat",
    "timeoutSeconds": 8.0,
    "temperature": 0.4,
    "controlMode": "fsm",
    "_comment": "复制本文件为 UserSettings/llm_config.json 并填入真实 key。endpoint 为 OpenAI 兼容服务根地址（不含 /chat/completions）。apiKey 首次加载后自动加密迁移到 apiKeyEncrypted 字段并清空明文。controlMode: fsm=本地状态机（默认）/ llm=AI 实时操控（M5）。UserSettings/ 已在 gitignore 中，真实配置不会入库。也可直接在游戏内 设置→AI 页签配置。"
}
